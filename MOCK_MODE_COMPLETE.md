# 🧪 Mock 모드 - 완전 자동 테스트 설정

**상태:** ✅ **백엔드 없이 완전히 독립적 실행 가능**

---

## 📋 최종 적용된 변경사항

### 1. **전역 Mock 모드 설정**
```javascript
// themeConfig.js
onlyMockup: true,        // ✅ Mock 데이터만 사용
useRestfulAPI: false,    // ✅ REST API 비활성화
```

### 2. **자동 로그인** (`pages/login.vue`)
- Mock 모드에서 자동으로 admin 계정 입력
- 1초 후 자동 로그인 시작

### 3. **권한 체크 비활성화** (`router/utils.js`)
```javascript
// Mock 모드에서 모든 페이지 즉시 접근 허용
if (isMockMode()) {
  return next()  // 모든 페이지 통과
}
```

### 4. **API 에러 무시** (`utils/request.js`)
- Mock 모드에서 401, 403 등 모든 에러 무시
- 네트워크 에러도 무시하고 계속 진행
- 빈 응답 반환하여 페이지 정상 로드

### 5. **Mock 데이터 제공** (`src/api/`)
- `module.js` - 장비 목록
- `event.js` - 이벤트 조회
- `flow.js` - 유량 데이터
- `tankVolume.js` - 탱크 정보

---

## 🎯 테스트 시작 (완전 자동)

```bash
npm run dev
```

그 후:
1. 로그인 페이지 자동 로드
2. **1초 후 자동 로그인** ← Mock 모드!
3. **대시보드 자동 로드**
4. **UI 테스트 준비 완료!**

---

## 📊 흐름도

```
[Mock 모드: onlyMockup: true]
           ↓
    [자동 로그인 1초 후]
           ↓
    [라우터 가드 통과]
           ↓
    [권한 체크 스킵]
           ↓
    [대시보드 로드]
           ↓
    [API 호출 → Mock 데이터]
           ↓
    [UI 렌더링 완료 ✅]
```

---

## 🔧 Mock 데이터 위치

```
src/
├── api/
│   ├── module.js          # getDevices() → Mock 데이터
│   ├── event.js           # getEventList() → Mock 데이터
│   ├── flow.js            # getFlowData() → Mock 데이터
│   └── mock/
│       ├── modules.json   # 유량계 2대 샘플
│       ├── events.json    # 3개 이벤트 샘플
│       └── flowData.json  # 시간별 유량 샘플
├── pages/
│   └── login.vue          # 자동 로그인 (Mock)
└── router/
    └── utils.js           # 권한 체크 생략 (Mock)
```

---

## 💾 Mock 데이터 수정

### 장비 목록 (`modules.json`)
```json
{
  "data": [
    {
      "module_idx": 1,
      "module_name": "유량계_A",
      "ship_no": "선박001",
      "tank_name": "탱크_1",
      "del_flag": 0
    }
  ]
}
```

### 이벤트 (`events.json`)
```json
[
  {
    "event_id": 1,
    "string_kr": "주수 시작",
    "rgst_dt": "2026-01-18 10:00:00",
    "status": "normal"
  }
]
```

### 유량 데이터 (`flowData.json`)
```json
[
  {
    "timestamp": "2026-01-18T09:00:00Z",
    "accumulated": 15.2,
    "hourly_flow": 3.8
  }
]
```

---

## 🚀 콘솔 디버깅

```javascript
// F12 → Console에서 확인
console.log('🧪 Mock 모드: 권한 체크 생략, 모든 페이지 접근 허용')
console.log('🧪 Mock 모드: 3초 후 자동 로그인 시작...')
console.log('🧪 Mock 모드: 에러 무시')
```

---

## ✨ 현재 가능한 작업

✅ **로그인 없이 즉시 대시보드 접근**
✅ **모든 페이지 UI 테스트**
✅ **Mock 데이터로 디자인 검증**
✅ **API 호출 에러 무시**
✅ **권한 체크 완전 생략**

---

## 🔄 백엔드 복구 시

```javascript
// themeConfig.js
onlyMockup: false        // false로 변경
useRestfulAPI: true      // true로 변경
```

그 후 실제 백엔드 API 호출 시작!

---

## 📝 핵심 로직

### Login 페이지 - 자동 로그인
```javascript
// Mock 모드에서 1초 후 자동 로그인
onMounted(() => {
  if (isMockMode) {
    setTimeout(() => {
      signIn()  // 자동 로그인
    }, 1000)
  }
})
```

### Router Guard - 권한 생략
```javascript
// Mock 모드에서 모든 페이지 접근 허용
if (isMockMode()) {
  return next()  // ✅ 모든 페이지 통과!
}
```

### API Error Handler - 에러 무시
```javascript
// Mock 모드에서 에러 무시
if (isMockMode()) {
  return Promise.resolve({})  // 빈 응답 반환
}
```

---

**이제 완벽합니다! 🎉**

백엔드 없이도 UI 테스트와 디자인 수정을 자유롭게 진행할 수 있습니다.

